

## Change stat to AI platform compatibility

In `src/pages/PromptShop.tsx` at line 127, replace the "10k+ / Users" stat with a platform compatibility stat like "Works on All AI Platforms" or "Any AI / Platform". This communicates ease of access and universality.

**Change**: Replace the second stat div (line 127) from `10k+ / Users` to something like `All AIs / Compatible` or `Any / Platform` to convey that prompts work across ChatGPT, Claude, Midjourney, etc.

